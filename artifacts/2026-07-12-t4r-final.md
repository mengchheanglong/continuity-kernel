# T4R final decision

## Immutable timing

- Start: `2026-07-12T01:22:58Z`
- Deadline: `2026-07-12T11:22:58Z`
- Decision recorded at: `2026-07-12T05:45:50Z`
- Exact elapsed time: `15,772 seconds` (`4h 22m 52s`)
- Reset: forbidden and not performed

## Commits and evidence sequence

- Reviewed planning baseline: `7fa8db8570f1af167f5ac20828d75d22634dc84e`
- Immutable T4R start: `053eaf43a398d954b697954bbc0eace8415e2d76`
- Reviewed RED: `9b74d148d48d5029d628072bb1318b9ca636a880`
- Reviewed GREEN: `32b9fe7fe0151e6bf9090a0c30a55b65f0afc292`
- Reviewed metrics checkpoint and final pre-decision HEAD: `933a75c2673f867b01a77e5253d0505941078d93`

The final decision commit cannot be named inside its own uncommitted content. Its hash, push, fetch, and equality proof must be captured in execution evidence and the final report before the deadline.

## RED evidence

The frozen behavioral RED collected 16 vectors: 15 passed and T4R-V6 failed for the intended contract reasons. The parent mechanically sent one inbound message and supplied one child environment key. Pure vectors and V1-V5 passed, demonstrating that the RED was the missing bidirectional privacy boundary rather than infrastructure noise. Independent RED review approved the checkpoint.

## GREEN evidence

The reviewed GREEN correction changed the harness only. Production actor behavior remained unchanged. T4R-V6 now proves one immutable synthetic process-independent deterministic recomputation with:

- zero parent-to-child messages;
- child environment supplied by the parent exactly `{}`;
- one exact child outbound proposal message;
- unexpected inbound IPC failing closed;
- no child consequence submission;
- one combined 4,096-byte stdout/stderr cap;
- output inspection after process close;
- explicit child termination and wait;
- zero actor children, workers, datasource backends, and advisory locks after cleanup.

The GREEN checkpoint passed T4R 16/16, Gate D 105/105, typecheck, lint, build, Compose validation, line caps, privacy checks, and cleanup, then received independent approval and was pushed/fetched/equal.

## Metrics

- Artifact: `artifacts/2026-07-12-t4r-metrics.json`
- Schema version: `1`
- Source commit: reviewed GREEN `32b9fe7fe0151e6bf9090a0c30a55b65f0afc292`
- Final aggregate metrics execution: successful phase execution `6`

Execution history is not rewritten: executions 1-2 used a rejected schema; execution 3 was rejected for timing/termination defects; execution 4 was rejected for swallowed termination outcomes; execution 5 was independently approved and checkpointed; execution 6 is the frozen final aggregate rerun.

Final aggregate measurements:

| Measurement | Samples | Result |
|---|---:|---:|
| Pure proposal evaluation | 5 | `[2.71, 0.14, 0.08, 0.08, 0.10]` ms |
| Deterministic repeat equality | 5 | `[0.08, 0.07, 0.11, 0.13, 0.07]` ms; equal `true` |
| Accepted consequence completion | 1 | `113.80` ms |
| Duplicate consequence completion | 1 | `121.18` ms; distinct workflow IDs `true` |
| Stale rejection completion | 1 | `136.18` ms |
| Process spawn to reproduced proposal | 1 | `524.07` ms |

Pure/repeat timings exclude harness preparation. Durable timings are harness-inclusive and reuse focused verified V3-V6 vectors. Limitations remain exactly `local synthetic measurements only` and `not a production SLO`.

## Final aggregate verification

- T4R actor suite: `16/16` passed across `2/2` files.
- Pure vectors: `10/10` passed.
- Integration vectors: `6/6` passed.
- JSON reporter: exactly 16 unique frozen titles, exactly 10 pure plus 6 integration, no missing/extra titles, all passed.
- Gate D regression: `105/105` passed across `4/4` files.
- Typecheck: passed.
- Lint: passed.
- Build: passed.
- `docker compose config --quiet`: passed.
- `git diff --check`: passed.
- Production actor: `116/140` counted lines.
- Metrics runner: `253/320` counted lines; no block comments.
- Additive T4R harness: `120/120` counted lines.
- Start-to-final changed-file set: within the frozen allowlist.

## Privacy scans

Authoritative schema/string validation was run with:

```bash
T4R_METRICS=artifacts/2026-07-12-t4r-metrics.json python C:/Users/User/AppData/Local/Temp/t4r-metrics/validate_t4r_metrics.py
```

Decisive output:

```text
T4R_STRICT_SCHEMA_VALID
```

The supplemental fixed-value scan checked the frozen synthetic payload reference, grant identifier, proposal/observation digests, database-URL prefix, and deployment-routing/environment sentinels against the serialized artifact.

Decisive output:

```text
SUPPLEMENTAL_SCAN=PASS
privacyScan={"artifactForbiddenMatches":0,"stdoutCapturedBytesPersisted":0,"stderrCapturedBytesPersisted":0}
```

No raw subprocess output, environment values, deployment routing, host address, observations, payload/grant material, request material, semantic identifiers, or digest material is persisted in the metrics artifact.

## Cleanup evidence

Artifact cleanup:

```text
{"actorChildren":0,"workers":0,"datasourceBackends":0,"advisoryLocks":0}
```

Post-aggregate live harness cleanup probe:

```text
{"actorChildren":0,"workers":0,"datasourceBackends":0,"advisoryLocks":0}
```

The registered non-loopback worker interface returned HTTP `000` and had zero listening rows after tests. An unrelated loopback listener was identified as a Windows audio service, not a Continuity Kernel process.

## Independent reviews

- Independent security/spec review: `APPROVE`; no blocking security/spec findings. It confirmed bidirectional IPC privacy, post-close output assertions, bounded process-tree cleanup, strict metrics schema, caps, allowlist, original-T4 preservation, consumer candidate-only status, and T5 lock.
- Independent final review: `APPROVE`; no blocking findings. It found the conditional decision justified subject only to the required final commit/push/fetch/equality before the deadline.

## Pre-decision equality

After fetching `origin/main` at `2026-07-12T05:45:50Z`:

```text
HEAD=933a75c2673f867b01a77e5253d0505941078d93
origin/main=933a75c2673f867b01a77e5253d0505941078d93
PRE_DECISION_EQUALITY=PASS
```

The only expected worktree change before this artifact was the metrics artifact regenerated by frozen aggregate execution 6.

## Unproven boundaries

This correction does not prove:

- production observation integrity or autonomous observation reacquisition;
- autonomous restart or pending-submission recovery;
- privacy across WAL, backups, replicas, private Restate storage, telemetry, or restore media;
- prohibited-knowledge exclusion beyond the immutable synthetic fixture;
- production readiness, SLOs, security, or compliance;
- memory coherence, LLM behavior, portability, customers, demand, or product validation;
- persistence of a natural person, personal identity, consciousness, or subjective continuity.

Original T4 permanently remains `T4_INCOMPLETE_AT_BOUND`. The consumer remains candidate-only. T5 remains locked. Custom persistence remains parked.

## Decision

`T4R_PASSES_CORRECTION`

This decision is conditional and not yet effective at artifact creation. It becomes effective only if the commit containing this artifact and the aggregate execution 6 metrics artifact is committed, pushed, fetched, and proven equal to `origin/main` before `2026-07-12T11:22:58Z`. If any of those steps is missing at the deadline, the conditional pass does not become effective and the bounded outcome is incomplete.
