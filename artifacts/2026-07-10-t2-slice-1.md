# T2 Slice 1 — Canonical Hashing

**Date:** 2026-07-10
**T2 window:** 2026-07-10T19:09:28Z through 2026-07-11T07:09:28Z
**Result:** GREEN with one TDD evidence limitation

## Scope

- exact pinned TypeScript project bootstrap;
- RFC 8785 canonical text;
- SHA-256 unpadded Base64URL digest;
- frozen completed-request, cancelled-request, and projection fixtures;
- strict I-JSON value validation;
- duplicate object-key rejection at raw JSON ingestion.

No database schema, DBOS workflow, migration, server, or custom persistence was added in this slice.

## Setup evidence

- Initial Codex run failed before writing files because `codex-cli 0.139.0` was too old for the configured model.
- Codex was upgraded to `0.144.1`.
- Docker Desktop was started and PostgreSQL 18.4 Linux/AMD64 image digest `sha256:0c49c0c906cb405ea65e70c284570fee91c7750ca9336369afc0edf4fce211db` was pulled successfully.
- `pnpm install --frozen-lockfile` reported: `Lockfile is up to date` and `Already up to date`.
- pnpm warned that esbuild install scripts were ignored. The test, typecheck, lint, and build commands below nevertheless passed.

## TDD evidence

Codex wrote `tests/canonical.test.ts` before the production module and attempted the RED phase. The background run then stopped advancing and was terminated at 671 seconds. Its RED console output was not retained in a trustworthy artifact.

Therefore:

```text
TEST-FIRST FILE ORDER: observed
INDEPENDENT RED CONSOLE OUTPUT: NOT PROVEN
```

No RED output is reconstructed or fabricated here.

## Independent GREEN verification

### Targeted test

```text
$ pnpm test -- tests/canonical.test.ts
Test Files  1 passed (1)
Tests       40 passed (40)
Duration    324ms
```

### Typecheck

```text
$ pnpm run typecheck
> tsc --noEmit
exit 0
```

### Lint

```text
$ pnpm run lint
> eslint src tests
exit 0
```

### Build

```text
$ pnpm run build
> tsc --noEmit
exit 0
```

## Recovered defects

The interrupted Codex run left a runtime-correct but type-invalid CommonJS/ESM import for `canonicalize@2.1.0` and two strict-lint failures for intentional infinite scanner loops. The verified fix:

- adds a narrow audited type bridge around the package's callable CommonJS runtime export;
- uses conditionless `for (;;)` loops;
- does not change fixture outputs or validation behavior.

## Frozen dependencies installed

```text
@dbos-inc/dbos-sdk 4.23.6
@dbos-inc/postgres-datasource 4.23.6
canonicalize 2.1.0
postgres 3.4.9
zod 4.4.3
@types/node 24.13.3
eslint 10.6.0
tsx 4.21.0
typescript 6.0.2
typescript-eslint 8.63.0
vitest 4.0.17
pnpm 10.32.1
```

## Production line count

`src/domain/canonical.ts`:

```text
TOTAL_LINES=88
NONBLANK_NONCOMMENT_LINES=79
SLICE_CAP_80=PASS
```

The global 300-line benchmark count will include this 79-line module.

## Files

- `.gitignore`
- `eslint.config.js`
- `package.json`
- `pnpm-lock.yaml`
- `src/domain/canonical.ts`
- `tests/canonical.test.ts`
- `tsconfig.json`

## Remaining work

This slice proves only deterministic canonicalization and frozen digest fixtures. It does not yet prove authorization ordering, canonical PostgreSQL commits, DBOS idempotency/recovery, bounded cancellation, role separation, or payload erasure.
