# T2b Final Alternative Decision — Restate/PostgreSQL

**Recorded:** 2026-07-11T06:51:47Z  
**T2b start:** 2026-07-11T02:43:48Z  
**Hard deadline:** 2026-07-11T08:43:48Z  
**Elapsed:** 4:07:59  
**Remaining at decision:** 1:52:01  
**Baseline/start commit:** `5cfc4b1065b407c7138d73d261e47319e9ec996d`  
**Preflight commit:** `dc36e50fde0332b4a9f4fbe05bd22be154137f87`

## Decision

```text
RESTATE_PASSES_V4
CUSTOM PERSISTENCE REMAINS PARKED
```

Pinned Restate Server 1.7.2 with TypeScript SDK/client 1.15.1 passes the unchanged external-kill V4 boundary within the frozen six-hour and 150-line bounds. PostgreSQL remains the canonical semantic authority. Restate provides durable invocation, redelivery, and attachment; the canonical receipt plus request hash provides semantic exactly-once behavior for the PostgreSQL transition.

This result advances the Restate/PostgreSQL direction to Gate D, where all six frozen vectors must be ported/run before foundation promotion. It does not claim that `ctx.run` makes arbitrary external database commits intrinsically exactly once, does not authorize custom persistence, and does not prove identity or subjective continuity.

## Frozen candidate and topology

| Item | Verified value |
|---|---|
| Restate Server | `1.7.2` |
| Server Linux/AMD64 image | `sha256:9c9b8dc71581c02ce1d85dd9928ed2728b92a5a43499880c86a1fa8b01fab86a` |
| TypeScript SDK | `@restatedev/restate-sdk@1.15.1` |
| TypeScript client | `@restatedev/restate-sdk-clients@1.15.1` |
| Workflow service | `ContinuityCommitT2bV1` |
| Runtime role | existing `continuity_app` role |
| Canonical authority | existing PostgreSQL `continuity.commit_case_transition` function and receipt |
| Runtime state store | not used for canonical state |
| Custom persistence | absent and unauthorized |

The tested Windows/Docker Desktop host had port `9080` occupied on loopback by Nahimic. The endpoint therefore registered through the host's verified LAN address using the test-only environment override:

```text
CK_RESTATE_DEPLOYMENT_URI=http://10.1.64.27:9080
```

The registered deployment reported HTTP/2, SDK `restate-sdk-typescript/1.15.1`, and the pinned service. This host routing workaround changes neither workflow input nor recovery semantics.

## Implementation

Candidate-specific production code:

- `src/alternative/restate/index.ts`
  - public Restate workflow and Node endpoint;
  - request hashing inside the handler;
  - one durable `ctx.run("canonical commit", ...)` step;
  - existing serializable PostgreSQL canonical transaction;
  - bounded SQLSTATE `40001` handling;
  - test-only IPC crash/recovery barriers that contain phase names only.
- `src/alternative/restate/client.ts`
  - public SDK workflow client;
  - caller-side semantic request-hash comparison;
  - attachment to the original invocation;
  - no manual resubmission during crash recovery.

No Restate private APIs, internal database/files, embedded state, custom journal, event store, projector, prepared transaction, or alternate canonical write path were added.

## V4 evidence

All tests use direct Node child processes with native IPC. The parent submits each invocation once, captures the original invocation ID, observes endpoint-process death, restarts only the endpoint, and attaches to the original invocation. Restate automatically redelivers pending work.

### Isolated V4A

```text
CK_RESTATE_DEPLOYMENT_URI=http://10.1.64.27:9080 \
  pnpm exec vitest run tests/restate-crash.test.ts \
  --no-file-parallelism --bail=1 \
  -t 'T2b-V4A Restate recovers exactly once after a pre-transaction worker kill'

1 passed | 3 skipped
Duration: 2.64s
```

Result: **PASS**. The parent killed the endpoint before PostgreSQL, observed prior version/digest with zero receipt/history, restarted into the deterministic recovery barrier, repeated the prior-state assertions, released recovery, and observed exactly one version-4 commit.

### Isolated V4B

```text
CK_RESTATE_DEPLOYMENT_URI=http://10.1.64.27:9080 \
  pnpm exec vitest run tests/restate-crash.test.ts \
  --no-file-parallelism --bail=1 \
  -t 'T2b-V4B Restate rolls back then recovers exactly once after a mid-transaction worker kill'

1 passed | 3 skipped
Duration: 50.15s
```

Result: **PASS**. The parent independently observed `application_name='continuity-kernel-restate'` with `wait_event='PgSleep'`, killed the endpoint, observed rollback/prior state and no receipt/history, restarted into the recovery barrier, then observed exactly one version-4 commit.

### Isolated V4C

```text
CK_RESTATE_DEPLOYMENT_URI=http://10.1.64.27:9080 \
  pnpm exec vitest run tests/restate-crash.test.ts \
  --no-file-parallelism --bail=1 \
  -t 'T2b-V4C Restate deduplicates recovery after commit before step completion'

1 passed | 3 skipped
Duration: 1.75s
```

Result: **PASS**. PostgreSQL showed one committed state/receipt/history transition before `ctx.run` returned; the parent killed the endpoint; Restate redelivered the ambiguous step; PostgreSQL returned the stored receipt; the final state still contained exactly one transition.

## Final Restate aggregate

```text
CK_RESTATE_DEPLOYMENT_URI=http://10.1.64.27:9080 \
  pnpm exec vitest run tests/restate-crash.test.ts \
  --no-file-parallelism --bail=1

Test Files  1 passed (1)
Tests       6 passed (6)
Duration    49.82s
```

Passing assertions:

1. T2b-V4A pre-transaction external kill/recovery;
2. T2b-V4B observed `PgSleep`, rollback, and recovery;
3. T2b-V4C post-commit/pre-step-completion deduplication;
4. same workflow key with a different request is rejected at the caller boundary;
5. a new workflow key cannot bypass the canonical command receipt;
6. optional payload bytes and plaintext-derived SHA-256 forms are absent from worker logs and supported `sys_journal` inspection.

The privacy assertion placed the frozen sentinel only in the deletable synthetic payload store, invoked Restate with the opaque `PayloadRef`, queried `sys_journal.raw` and `entry_json` through the supported Admin SQL endpoint, checked plaintext and derived hash forms, checked child logs, and deleted the optional payload object.

## PostgreSQL and static gates

```text
pnpm run test:db     -> 32 passed, exit 0
pnpm run typecheck   -> exit 0
pnpm run lint        -> exit 0
pnpm run build       -> exit 0
git diff --check     -> exit 0
docker compose config --quiet -> exit 0
```

The database suite preserves application-role privilege negatives, canonical receipt semantics, request-hash equality, authorization ordering, expected-version conflict, and frozen projections.

Frozen integrity checks:

```text
FROZEN_FILES=UNCHANGED
PRIVATE_API_OR_STATE_ACCESS=NONE
OPTIONAL_BYTES_IN_SOURCE=NONE
OPTIONAL_BYTES_IN_RESTATE_LOGS=NONE
```

Unchanged files checked directly included:

- `docs/conformance-vectors.md`;
- `docs/invariants.md`;
- `docs/architecture/t2b-preflight.md`;
- `tests/db-fixture.ts`;
- `src/domain/canonical.ts`;
- `migrations/001_continuity.sql`.

## Full historical aggregate

The full repository command intentionally retains the rejected DBOS counterexample:

```text
CK_RESTATE_DEPLOYMENT_URI=http://10.1.64.27:9080 pnpm run test

Test Files  1 failed | 4 passed (5)
Tests       2 failed | 87 passed (89)
```

All six Restate tests passed in this run. The two failures remained confined to `tests/dbos-crash.test.ts` at the previously frozen DBOS pre-commit boundaries:

- `DataSource continuity-domain is not initialized.` during pre-transaction recovery;
- Windows/Docker backend disappearance bound during the mid-transaction DBOS counterexample.

These are the preserved T2 negative evidence documented in `artifacts/2026-07-11-t2-final.md`, not Restate regressions. The DBOS result remains historical and unchanged.

## Reproducible line counts

Hand-written nonblank, noncomment lines:

```text
src/domain/canonical.ts=79
src/alternative/restate/client.ts=30
src/alternative/restate/index.ts=95
migrations/001_continuity.sql=124

CANDIDATE=125/150
COMBINED_SURVIVING_DIRECTION=328
```

Candidate-specific Restate code is 25 lines below the frozen cap. Tests, lockfiles, Compose, documentation, and artifacts are excluded exactly as specified in the preflight.

## Scope audit

- Frozen expected outcomes changed: **no**.
- Canonical PostgreSQL transaction replaced: **no**.
- Canonical state read from Restate embedded state: **no**.
- Invocation manually resubmitted after endpoint restart: **no**.
- Original invocation ID captured and attached: **yes**.
- Runtime database/files or private protocol accessed: **no**.
- Optional payload bytes sent to Restate: **no**.
- Custom persistence implemented or authorized: **no**.
- Foundation promoted from V4 alone: **no**.

## Next gate

Schedule Gate D to port and run all six frozen conformance vectors against the surviving Restate/PostgreSQL direction. Keep PostgreSQL receipts authoritative, preserve the semantic/hash/privacy/authorization contracts unchanged, and keep custom persistence parked unless a future dated gate provides new evidence.
