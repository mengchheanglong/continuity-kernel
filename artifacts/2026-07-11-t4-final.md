# T4 Final — One Deterministic Actor

**Decision recorded:** 2026-07-11T18:47:37Z  
**T4 start:** 2026-07-11T15:46:31Z  
**Hard deadline:** 2026-07-11T19:46:31Z  
**Clock reset:** no  
**Elapsed at decision:** `03:01:06`  
**Remaining at decision:** `00:58:54`  
**Foundation decision:** `GATE_D_PASSES_FOUNDATION_V3` (`3bf0f35e9b904325b001bdea534bd8b35fcf6bb1`)  
**T4 planning/preflight commit:** `ecd0d062b6e36a40ab62a5fd6142a9b5678a30e4`  
**T4 start commit:** `f94cc607325a40cd1ee6f54c342771f529e301a2`  
**Pure-actor baseline commit:** `cf95b1fc63c5112837dcc433f46163bfc72bb795`  
**Final decision commit:** the commit containing this artifact; pure-actor baseline remains `cf95b1fc63c5112837dcc433f46163bfc72bb795`

## Decision

```text
T4_PASSES_DETERMINISTIC_ACTOR
CUSTOM PERSISTENCE REMAINS PARKED
T5_AND_LATER_REMAIN_LOCKED
```

Stop for T4 review. This decision does not begin T5, memory, models, multi-agent worlds, inspectors, portability, or custom persistence.

## What was proven

One pure deterministic actor converts a strict permitted-observation representation into one typed `resolve_case` proposal and uses the existing approved Restate/PostgreSQL path for accepted or rejected consequences.

Boundaries preserved:

1. **Synthetic observation provenance** — test-only trusted join over case + unrevoked grant; not a production observation API.
2. **Local actor consistency** — representation/schema/declared-scope fail-fast only; not authorization.
3. **Authoritative authorization** — existing `continuity.commit_command` decides grant, scope, version, revocation, and state.

## Vector evidence

| Vector | Result |
|---|---|
| T4-V1 deterministic repetition | PASS |
| T4-V2A local representation/consistency rejection + no submit | PASS |
| T4-V2B authoritative authorization handoff (revoked/wrong actor/case/namespace/stale auth version) | PASS |
| T4-V3 accepted causal trace | PASS |
| T4-V4 duplicate proposal / distinct workflow keys / one receipt | PASS |
| T4-V5 stale proposal after competing transition | PASS |
| T4-V6 process-loss deterministic reproduction (not autonomous recovery) | PASS |

Exactly **16** collected T4 tests: 10 pure-selector/adapter + 6 Restate integration.

## Production bounds

| Bound | Result |
|---|---|
| T4-specific production files | `src/actor/deterministic.ts`, `src/actor/execute.ts` only |
| Counted nonblank non-comment lines | **116 / 140** |
| Dependencies / migrations / schema / compose changes | none |
| Gate-D production / frozen contract weakening | none |

Line-count command (preflight):

```text
node -e "const fs=require('fs');let c=0;for (const f of ['src/actor/deterministic.ts','src/actor/execute.ts']){for (const line of fs.readFileSync(f,'utf8').split(/\r?\n/)){const t=line.trim();if(!t||t.startsWith('//'))continue;c++}};console.log('counted_lines='+c)"
→ counted_lines=116
```

## Final verification pack

Executed before the hard deadline with:

```text
CK_RESTATE_DEPLOYMENT_URI=http://172.23.32.1:9080
```

```text
pnpm run test:t4
  2 files passed
  16 tests passed
  duration ~14s

pnpm run test:gate-d
  4 files passed
  105 tests passed
  duration ~113s

pnpm run typecheck                 PASS
pnpm run lint                      PASS
pnpm run build                     PASS
docker compose config --quiet      PASS
production actor line count        116/140
```

## Topology

Single Windows host; Node Restate endpoint on port `9080`; Restate Server `1.7.2` ingress `8080` / Admin `9070`; PostgreSQL on `127.0.0.1:55432`. Deployment URI uses the verified Docker/WSL bridge address `172.23.32.1:9080` consistent with Gate D.

## Implementation surface

### Production (counted)

- `src/actor/deterministic.ts` — strict observation schema, fixed rule/goal, pure `selectProposal`, data-free `ActorDecisionError` codes, deterministic `commandId` / `proposalDigest`.
- `src/actor/execute.ts` — thin adapter calling only existing `submitCommand`.

### Test-only harness (not counted)

- `tests/deterministic-actor.test.ts` — 10 pure tests
- `tests/restate-actor.test.ts` — 6 integration tests
- `tests/actor-observation-fixture.ts` — trusted synthetic observation join + worker/admin harness
- `tests/actor-child.ts` / `tests/actor-child-runner.ts` — process-loss proposal emission with sanitized IPC `{type, commandId, proposalDigest}`
- `package.json` script `test:t4` exactly as frozen in preflight

## Non-claims

T4 does not prove intelligence, autonomy, memory, learning, personal identity, consciousness, subjective continuity, a production observation service, general prohibited-knowledge confinement, autonomous restart, observation reacquisition, or pending-submission recovery. Gate-D V4 remains authoritative for runtime recovery.

Optional local measurements (`measure:t4`) were not required for the pass decision under the frozen priority rule (mandatory vectors and Gate-D regression over measurement polish) and were not recorded in this pack.

## Scope audit

- No custom persistence, event store, projector, or new canonical table/column/role.
- No LLM, memory system, multi-agent world, inspector, UI, or portability layer.
- No private Restate storage/API use from `src/actor/`.
- No wall clock, randomness, network, or database access in pure proposal selection.
- Parent-supplied observation binding enforces T4-V6 identical-observation reproduction; mismatched child observation fails closed with `ACTOR_CHILD_OBSERVATION_MISMATCH`.

## Next

Mission Control must synchronize `.active/*` and `TASKS.md` to this decision. T5 remains locked until a separate evidence-gated approval. Recommended next improvement after review: a bounded **consumer gate** (external case study, thin second consumer, or public runnable harness) before opening memory/model work.
